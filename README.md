# `@arquimea/prettier-config`

>Arquimea's [Prettier](https://prettier.io) config.
>https://prettier.io/docs/en/options.html
## Usage

**Install**:

```bash
$ yarn add --dev @arquimea/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@arquimea/prettier-config"
}